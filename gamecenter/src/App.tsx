import ListGroup from "./components/ListGroup";

// App component
function App() {
  return (
    // Note that each component is independent of another one. (repeat <ListGroup /> to see)
    <div>
      <ListGroup />
    </div>
  );
}

// Always export components
export default App;
